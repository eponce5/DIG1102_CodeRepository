

    var userChoice = prompt("heads or tails?");

    var computerChoice = Math.random();
    Math.floor(Math.random() * 2);
    if (Math.random() < 0.5) {
        println("heads!");
      } else {
        println("tails!");
      }
    console.log(y)
    console.log(computerChoice);
    var heads = computerChoice <= 0;
    var tails = computerChoice >= 1;

    var val = randomInt(0, 1);
if (val === 0 ) {
  println("Heads");
  var val = randomInt(0, 1);
if (val === 2 ) {
  println("Tails");
   
}
var userChoice = prompt("What year were you born?");
    const birthYear = 2001; n 
} if (( 2021 - birthYear) < 21 ) {
 alert ("Sorry, you are not old enough to drink in the US");

}
